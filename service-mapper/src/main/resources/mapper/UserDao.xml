<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.tlh.admin.dao.UserDao">

    <insert id="insertUser" parameterType="cn.tlh.admin.common.pojo.User">
        insert into user (username,password,email)
         values (#{username},#{password},#{email})
    </insert>

    <select id="queryByEmail" resultType="cn.tlh.admin.common.pojo.User">
        select *
        from user
        where email = #{email}
    </select>

</mapper>